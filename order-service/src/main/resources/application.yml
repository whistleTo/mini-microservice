server:
  port: 8082


spring:
  application:
    name: order-service  # 这个不能为空！
  datasource:
    url: jdbc:mysql://localhost:3307/demo_cloud?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true